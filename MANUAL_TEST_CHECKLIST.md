# 手動硬體與環境驗收檢核表 (Manual Test Checklist)

實體硬體與作業系統互動無法由單元測試替代；音訊介面、多螢幕、實體拔插、Windows DPI/鎖定、睡眠喚醒與長時間錄影都必須依照本檢核表人工驗證。未填妥證據的項目一律視為「尚未驗證」，不得推定為 `PASS`。

每個案例完成後，請複製並填寫以下證據區塊：

```text
OpenCam 版本：
Commit：
測試日期與時區：
OS / Build：
硬體與連接方式：
執行者：
結果：PASS / FAIL / BLOCKED
產出物 / Artifact 路徑：
Log 路徑：
備註與重現步驟：
```

---

## v0.2.3 安全修補發布閘門 (REQUIRES MANUAL VALIDATION)

使用包含本次 C# 與 Swift helper 修改的同一份測試包，不可混用舊 helper。
macOS 必須解鎖並授予螢幕／系統音訊與麥克風權限；Windows 需使用原生 Windows 主機。
以下項目尚未填入證據前不得視為通過，也不可用合成音訊測試替代真實裝置測試。

1. 在 macOS 及 Windows 各測試無聲音、僅系統聲音、僅麥克風、兩者皆開四種模式，每種至少 5 分鐘。保存 MP4、ffprobe 資訊與 Log；檢查未勾選的來源沒有錄入、音訊清晰、無斷裂且音畫同步。
2. 在雙音源模式暫停，再分別切換音源並繼續。確認波形、實際聲音及分段合併一致，停止後無 helper／FFmpeg 殘留。重複開始／停止，並測試硬體編碼失敗後的軟體編碼回退。
3. 開啟兩個實例，分別錄製測試內容；對其中一個測試工作階段強制結束 Recorder。重新開啟後救援，確認原始 MKV 保留、MP4 可播放、不覆寫既有檔案，另一實例不受影響。只中斷專用測試錄影。
4. 使用一般實體儲存目錄、含空白與中文的目錄各測試一次；符號連結目錄應顯示安全失敗且不寫入連結目標。Windows 執行目錄替換／連結／硬連結自動化案例，確認祖先目錄鎖定與檔案共享行為。
5. 確認正常 UI 啟動可控制錄影，未經合法啟動通道的 daemon 被拒絕；「關於本程式」顯示非空白 0.2.3 版本標籤。拒絕權限時應有清楚錯誤，重新授權後能恢復錄製。

任何開始／停止卡住、無聲音、波形與輸出不一致、資料遺失或未授權控制都記為 FAIL；鎖定畫面、缺少裝置或缺少 OS 則記為 BLOCKED。每項記錄文件開頭的證據欄位，不能僅填總體 PASS。

---

## 檢核項目一：麥克風拔除容錯測試 (TEST-AUDIO-MIC-REMOVAL)

- **前置條件**:
  - 外接 USB 麥克風已連接並被識別為預設或指定錄音設備。
  - 專案程式以 `System Audio + Microphone` 模式啟動。
- **測試步驟**:
  1. 點擊「開始錄影」。
  2. 說話與播放系統音樂錄製 60 秒。
  3. 直接拔除 USB 麥克風。
  4. 觀察錄影是否仍在繼續，再錄製 60 秒。
  5. 點擊「停止錄影」並等待 Remux 完成。
- **預期結果 (PASS)**:
  - 軟體不崩潰，UI 顯示麥克風已離線警告。
  - 輸出之 MP4 影片正常可播，前 60 秒包含麥克風聲音，後 60 秒持續錄製畫面與系統聲音。
- **失敗條件 (FAIL)**:
  - Recorder 崩潰退出、錄影檔案為 0 byte 或畫面停格凍結。
- **驗證紀錄**: 複製文件開頭的證據區塊，完整填寫後附上影片、Session 與 Log 路徑。

---

## 檢核項目二：多螢幕與負座標測試 (TEST-DISPLAY-MULTI-MONITOR)

- **前置條件**:
  - 連接雙螢幕，副螢幕設定在主螢幕左側（產生負 X 座標 `e.g. -1920, 0`）。
- **測試步驟**:
  1. 測試選擇主螢幕錄影 1 分鐘，檢查輸出。
  2. 測試選擇副螢幕錄影 1 分鐘，檢查輸出。
  3. 測試使用「自訂區域」拉取跨越或位於副螢幕的矩形區域。
- **預期結果 (PASS)**:
  - 所選螢幕內容正確對應，自訂區域無偏移、無黑邊拉伸。
- **驗證紀錄**: 複製文件開頭的證據區塊，記錄每台螢幕型號、排列、縮放與各輸出檔路徑。

---

## 檢核項目三：高 DPI 縮放測試 (TEST-DISPLAY-DPI-SCALING)

- **前置條件**:
  - 將螢幕縮放比例依序設定為 100%、125%、150%、200%。
- **測試步驟**:
  1. 在各縮放級別下開啟自訂區域選取框。
  2. 圈選已知視窗大小（如 800x600）。
  3. 錄製並以 `ffprobe` 檢視視訊流的寬度與高度。
- **預期結果 (PASS)**:
  - 選取框顯示之寬高數值與實際像素解析度符合物理尺寸換算，無因 DPI 造成之裁切或倍率縮放偏移。
- **驗證紀錄**: 複製文件開頭的證據區塊，記錄每個 DPI 比例、截圖與 ffprobe 輸出路徑。

---

## 檢核項目四：Windows 鎖定與解鎖測試 (TEST-POWER-LOCK-UNLOCK)

- **測試步驟**:
  1. 開始錄影 30 秒。
  2. 按下鍵盤 `Win + L` 進入鎖定畫面等待 30 秒。
  3. 解鎖進入桌面，再錄製 30 秒。
  4. 正常停止錄影。
- **預期結果 (PASS)**:
  - 鎖定期間與解鎖後 Recorder 行程不崩潰，結束後輸出可正常播放之 MP4。
- **驗證紀錄**: 複製文件開頭的證據區塊，附上 Windows build、影片、Session 與 Log 路徑。

---

## 檢核項目五：系統休眠與喚醒測試 (TEST-POWER-SLEEP-RESUME)

- **測試步驟**:
  1. 開始錄影。
  2. 讓電腦進入睡眠模式 (Sleep) 1 分鐘。
  3. 喚醒系統並登入。
  4. 觀察錄影狀態並點擊停止。
- **預期結果 (PASS)**:
  - 系統喚醒後錄影安全處理（繼續錄影或已安全保存休眠前之內容），檔案無損毀。
- **驗證紀錄**: 複製文件開頭的證據區塊，附上電源設定、影片、Session 與 Log 路徑。

---

## 檢核項目六：macOS 內建麥克風連續性測試 (TEST-MACOS-BUILTIN-MIC-CONTINUITY)

- **前置條件**:
  - 使用打包後的 `OpenCam.app`，並已授予麥克風與螢幕錄製權限。
  - 音訊模式選擇「只有麥克風」，裝置為 Mac 內建麥克風。
- **測試步驟**:
  1. 開始自訂區域錄影並持續說話至少 30 秒。
  2. 正常停止並等待 MP4 完成。
  3. 播放 MP4，確認人聲沒有週期性中斷、爆音或明顯變速。
  4. 使用 `ffprobe` 確認音訊為 48 kHz AAC，且音訊時間長度接近影片。
  5. 使用 `silencedetect=noise=-70dB:d=0.08` 檢查；持續說話區段不得反覆出現固定約 106.7 ms 的靜音塊。
- **預期結果 (PASS)**:
  - 人聲連續清晰，沒有舊版每隔約一秒遺失 5,120 個樣本的斷音。
  - Log 中的 FFmpeg 命令使用 `-f s16le ... microphone.pcm`，不得使用 `-f avfoundation -i ":0"` 作為麥克風輸入。
- **失敗條件 (FAIL)**:
  - 固定長度靜音塊重現、`OpenCam.Microphone` 非預期退出，或錄影因此中止。
- **驗證紀錄**: 複製文件開頭的證據區塊，附上 MP4、Session、Log、ffprobe 與 silencedetect 輸出路徑。

---

## 檢核項目七：錄影啟動期間設定鎖定 (TEST-UI-PREPARING-SETTINGS-LOCK)

- **測試步驟**:
  1. 在待機狀態確認錄影範圍、FPS、編碼器、音訊與儲存位置可調整。
  2. 點擊「開始錄影」，在初始化提示仍顯示時立即嘗試調整上述選項。
  3. 錄影開始後再次嘗試調整；接著暫停錄影並分別測試音訊、游標與其他設定。
  4. 停止錄影；另測一次啟動失敗的情境。
- **預期結果 (PASS)**:
  - 點擊開始後設定面板立即鎖定，初始化與錄影期間皆不可修改。
  - 暫停期間只開放系統聲音、麥克風（含裝置）與游標樣式，變更會套用到下一段錄影。
  - 暫停期間錄影範圍、FPS、編碼器與儲存位置仍維持鎖定。
  - 停止完成或啟動失敗回到待機後，設定面板恢復可操作。
- **失敗條件 (FAIL)**:
  - 初始化期間仍可改動任何錄影設定，造成 UI 顯示值與實際錄製參數不一致。
- **應保存證據**:
  - MP4、對應 Session MKV、`app-YYYYMMDD.log` 與 `ffprobe`/`silencedetect` 輸出。
- **驗證紀錄**: 複製文件開頭的證據區塊，另附上各狀態的介面截圖或螢幕錄影路徑。
